<h1>Listing pers</h1>

<table>
  <tr>
    <th>Nombre</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pers.each do |per| %>
  <tr>
    <td><%= per.nombre %></td>
    <td><%= link_to 'Show', per %></td>
    <td><%= link_to 'Edit', edit_per_path(per) %></td>
    <td><%= link_to 'Destroy', per, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Per', new_per_path %>
