<h1>Nuevo animal</h1>



<table class="tabla">
  <tr>
    <th>Zona animal1</th>
    <th>Id relacion</th>
    <th>Animal1</th>
    <th>Nombre Animal1</th>
    <th>Foto Animal1</th>
    <th>Zona animal2</th>
    <th>Nombre Animal2</th>
    <th>Foto Animal2</th>
    <th>Relacion</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>


  <tr class="modo1">
    <td><%= @relacion_animal.animal.chenil.zona_id %>
    <td><%= @relacion_animal.id %></td>
    <td><%= @relacion_animal.animal1_id %></td>
    <td><%= @relacion_animal.animal.nombre %></td>

    <% if @relacion_animal.animal.images.empty? %>
      <td><%= image_tag 'logo.png'%></td>
    <% else %>
      <td><%= image_tag @relacion_animal.animal.images.first.image.url(:thumb) %>
    <%end%>

    <td><%= @relacion_animal.animal2.zona_id %></td>
    <td><%= @relacion_animal.animal2.nombre %></td>
   <% if @relacion_animal.animal2.images.empty? %>
    <td><%= image_tag 'logo.png'%></td>
   <% else %>
    <td><%= image_tag @relacion_animal.animal2.images.first.image.url(:thumb) %>
   <%end%>
    <td><!--= image_tag relacion_animal.animal2.foto.url(:thumb) -->
    <td><%= @relacion_animal.relacion %></td>
    <!--td>< link_to 'Show', @relacion_animal ></td-->
<% if (current_persona.perfil.ocupacion_id == 1 || current_persona.perfil.ocupacion_id == 2 || current_persona.perfil.ocupacion_id == 3) %>
    <td><%= link_to 'Destroy', @relacion_animal, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>

</table>

<br />
<%= form_for(@relacion_animal) do |f| %>

  <!--div>
  <select name="@relacion_animal[relacion]"> 
   < @tiposRel.each do |tipo| %> 
       <option value="< tipo.id %>" 
         < 'selected' if tipo.id == @relacion_animal.relacion %>>
         < tipo.id %> 
       </option> 

       < @relacion_animal.relacion=tipo.nombre %>	

   < end %> 
  </select></div-->
 

 
  <!--div>
  <select name="f[relacion]"> 
       <option></option> 
       <option>pelea</option> 
       <option>regular</option> 
       <option>sin info</option> 
       <option>bien</option> 

  </select></div-->
 
<%= f.select :relacion, ["", "pelea","regular", "sin info", "bien"] %>

<br />
<br />


  <div class="actions">
    <%= f.submit %>
  </div>

<%end%>

<br />
<br />


<%= link_to 'Show', @relacion_animal %> |
<%= link_to 'Back', relacion_animals_path %>

