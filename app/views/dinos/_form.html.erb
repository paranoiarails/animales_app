<%= form_for(@dino) do |f| %>
  <% if @dino.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dino.errors.count, "error") %> prohibited this dino from being saved:</h2>

      <ul>
      <% @dino.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :dina_id %><br />
    <%= f.number_field :dina_id %>
  </div>
  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :raza %><br />
    <%= f.text_field :raza %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
