<%= form_for(@dina) do |f| %>
  <% if @dina.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dina.errors.count, "error") %> prohibited this dina from being saved:</h2>

      <ul>
      <% @dina.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :edad %><br />
    <%= f.number_field :edad %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
