<h1>New diario</h1>

  <!-- @diario.zona_id=@zona %>
  < @diario.fecha=@fecha %>
  < @diario.save -->

<%= form_for(@diario) do |f| %>
  <% if @diario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@diario.errors.count, "error") %> prohibited this diario from being saved:</h2>

      <ul>
      <% @diario.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= @zona.nombre %>
  <%= $fecha %>

  </br>
  </br>

<div class="roundB">
 <%= "Para esta zona y esta fecha no existen tareas ni informes guadados con anterioridad, deseas crear tareas o informes nuevos?  "%> 
</div>

  </br>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', diarios_path %>
