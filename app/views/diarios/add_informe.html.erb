<p>
  <b>Zona:</b>
  <%= @diario.zona.nombre %>
</p>

<p>
  <b>Fecha:</b>
  <%= @diario.fecha %>
</p>

<div class="roundB">
<ul>
<p>
  <b>Informe:</b>
  <%= simple_format h @diario.informe %>
</p>
</ul>
</div>

<%= form_for(@diario) do |f| %>
  <% if @diario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@diario.errors.count, "error") %> prohibited this diario from being saved:</h2>

      <ul>
      <% @diario.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :"añadir a informe: " %><br />
    <%= f.text_area :nuevoI %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
