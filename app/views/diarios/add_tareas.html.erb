<p>
  <b>Zona:</b>
  <%= @diario.zona_id %>
</p>

<p>
  <b>Fecha:</b>
  <%= @diario.fecha %>
</p>

<div class="roundB">
<ul>
<p>
  <b>Tareas:</b>
  <%= simple_format h @diario.tareas %>
</p>
</ul>
</div>

<%= form_for(@diario) do |f| %>
  <% if @diario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@diario.errors.count, "error") %> prohibited this diario from being saved:</h2>

      <ul>
      <% @diario.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :"nuevas tareas" %><br />
    <%= f.text_area :nuevo %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
